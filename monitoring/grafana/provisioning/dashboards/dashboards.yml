apiVersion: 1

providers:
  - name: 'Default Dashboards'
    orgId: 1
    folder: 'ClimaSentinel Backend'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # Grafana will load all json dashboard files from this directory
      path: /etc/grafana/provisioning/dashboards
